public class Motorcycle extends Vehicle {
    public Motorcycle(String modelName, int maxSpeed, double fuelConsumptionIndex ) {
        super(modelName, maxSpeed, fuelConsumptionIndex , TypeOfVehicle.MOTORCYCLE);
    }
}
